server:
  port: ${SERVER_PORT}

eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-ms-service:8761/eureka/
